"use strict";
// // import { removeQuoteMarks } from '../removeQuoteMarks/removeQuoteMarks';
// import { template } from 'lodash';
// import { removeQuoteMarks } from '../removeQuoteMarks';
// import { SPEC_BLOCK_TEMPLATE, DEFAULT_SPEC } from '../constants';
// // export const specBlock = template(SPEC_BLOCK_TEMPLATE);
// const setSpecBlock = (spec: string, specTemplate: any) => specTemplate({ spec: removeQuoteMarks(spec).trim() });
// export const setSpecItems = (specs: string[], specBlock: any) => {
//   if (specs.length === 0) {
//     return setSpecBlock(DEFAULT_SPEC, specBlock);
//   }
//   // {
//   let specBlocks = '';
//   for (const spec of specs) {
//     specBlocks += setSpecBlock(spec, specBlock);
//   }
//   return specBlocks;
//   // }
// };
