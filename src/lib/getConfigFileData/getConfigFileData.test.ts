import { getConfigFileData } from './getConfigFileData';

describe('getConfigFileData', () => {

  it('reads data from config file', () => {
    // create a dummy file, read data from file
    expect.hasAssertions();
  });

  it('returns undefined if no file is found', () => {
    expect.hasAssertions();
  });

});

